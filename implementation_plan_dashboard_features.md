# แผนการพัฒนาระบบ: ฟีเจอร์ Dashboard และระบบต่างๆ

เอกสารนี้แสดงรายละเอียดขั้นตอนการพัฒนาฟีเจอร์ที่เหลือใน Dashboard ของ GamEdu โดยเน้นไปที่บทบาท "ครู (Teacher)" และ "นักเรียน (Student)" ตาม UI ปัจจุบัน

## 1. ภาพรวมสถานะปัจจุบัน

| ฟีเจอร์ | สถานะ UI | สถานะการทำงาน | ความสำคัญ |
| :--- | :--- | :--- | :--- |
| **สร้างชุดคำถาม (Create Set)** | ✅ เสร็จแล้ว | ✅ เสร็จแล้ว | - |
| **ชุดคำถามของฉัน (My Sets)** | ✅ เสร็จแล้ว | ✅ เสร็จแล้ว | - |
| **โฮสต์เกม (Host Game)** | ✅ เสร็จแล้ว | ⚠️ พื้นฐาน (ต้องปรับปรุง Logic) | สูง |
| **รายงานผล (Reports)** | ✅ มีแค่ปุ่ม | 🔴 ยังไม่มีระบบ | สูง |
| **มอบหมายงาน (Assignments)** | ✅ มีแค่ปุ่ม | 🔴 ยังไม่มีระบบ | สูง |
| **ค้นหา (Discover)** | ✅ มีแค่ปุ่ม | 🔴 ยังไม่มีระบบ | ปานกลาง |
| **ร้านค้า (Market)** | ✅ มีแค่ปุ่ม | 🔴 ยังไม่มีระบบ | ปานกลาง |
| **ของสะสม (Collection)** | ✅ มีแค่ปุ่ม | 🔴 ยังไม่มีระบบ | ต่ำ |
| **ตั้งค่า (Settings)** | ✅ พื้นฐาน | ⚠️ รอระบบแก้ไขโปรไฟล์ | ต่ำ |

---

## 2. ระยะที่ 1: ศูนย์ควบคุมสำหรับครู (Phase 1: Teacher's Control Center) - **ความสำคัญสูง**
*เป้าหมาย: ช่วยให้ครูติดตามผลการเรียนและมอบหมายงานได้สะดวก*

### 2.1. รายงานผลการสอน (`/dashboard/reports`)
**วัตถุประสงค์:** ให้ครูดูประวัติการเล่นเกมของนักเรียนและผลคะแนนย้อนหลังได้

-   **ฐานข้อมูล (Database):**
    -   ใช้ Model `GameHistory` ที่มีอยู่แล้ว
    -   ตรวจสอบ field `players` (JSON) ให้แน่ใจว่าเก็บข้อมูลครบถ้วน: `nickname`, `score`, `correctAnswers`, `incorrectAnswers`
-   **งานฝั่ง UI:**
    -   สร้างหน้า `src/app/dashboard/reports/page.tsx`: รายการเกมที่ผ่านมา (เรียงตามวันที่)
    -   สร้างหน้า `src/app/dashboard/reports/[id]/page.tsx`: หน้ารายละเอียดของแต่ละเกม
    -   **กราฟ (Charts):** ใช้กราฟง่ายๆ แสดง "ความแม่นยำของทั้งห้อง" หรือ "5 อันดับแรก"
    -   **ตาราง (Table):** รายชื่อนักเรียนทุกคนในรอบนั้นพร้อมคะแนน

### 2.2. การบ้านแกและการมอบหมายงาน (`/dashboard/homework`)
**วัตถุประสงค์:** ให้นักเรียนสามารถเข้ามาทำแบบทดสอบได้เองตามเวลาที่กำหนด (ไม่ต้องเล่นพร้อมกันสดๆ)

-   **ฐานข้อมูล (New Model):**
    -   สร้าง Model `Assignment` ใน Prisma
    -   ข้อมูลที่เก็บ: `id`, `setId`, `classId` (ถ้ามี), `deadline` (วันครบกำหนด), `code` (รหัสเข้าเล่น), `creatorId`
    -   สร้าง Model `AssignmentResult` เพื่อเก็บผลการทำของนักเรียน
-   **งานฝั่ง UI:**
    -   **สร้างงาน (Create Assignment):** หน้าต่างสำหรับเลือก "ชุดคำถาม" + "วันส่ง" + "กดสร้างรหัส"
    -   **มุมมองนักเรียน:** ช่อง "กรอกรหัส (Enter Code)" ต้องรองรับทั้งรหัสเกมสดและรหัสการบ้าน
    -   **มุมมองครู:** หน้าติดตามว่าใครส่งงานแล้วบ้าง ได้คะแนนเท่าไหร่

---

## 3. ระยะที่ 2: เกมและการมีส่วนร่วม (Phase 2: Gamification) - **ความสำคัญปานกลาง**
*เป้าหมาย: ทำให้นักเรียนสนุกและอยากกลับมาใช้งานเรื่อยๆ*

### 3.1. ค้นหาเนื้อหาสาธารณะ (`/dashboard/discover`)
**วัตถุประสงค์:** ให้ผู้ใช้ค้นหาและคัดลอกชุดคำถามจากชุมชน (Community) ได้

-   **ฐานข้อมูล:**
    -   ทำ Index ให้กับ `QuestionSet` ในส่วน `title` และ `isPublic`
-   **งานฝั่ง UI:**
    -   **หน้าค้นหา:** ช่องค้นหา + ตัวกรอง (วิชา, ระดับชั้น - *ในอนาคต*)
    -   **ดูตัวอย่าง (Preview):** ดูคำถามในชุดนั้นๆ ได้ (แต่แก้ไขไม่ได้)
    -   **ทำซ้ำ (Duplicate):** ปุ่ม Copy ชุดคำถามมาเป็นของตัวเอง (เปลี่ยน `creatorId` เป็นเรา)

### 3.2. ตลาดและเศรษฐกิจในเกม (`/dashboard/market`)
**วัตถุประสงค์:** สร้างแรงจูงใจในการสะสม Token

-   **ฐานข้อมูล:**
    -   ผู้ใช้มี `tokens` อยู่แล้ว
    -   กำหนด "Packs" (กล่องสุ่ม) แบบง่ายๆ (อาจจะ Hardcode ไว้ก่อน หรือใส่ DB) เช่น "ชุดสัตว์โลก", "ชุดเทคโนโลยี"
    -   เก็บข้อมูล `unlockedBlooks` ใน User model
-   **งานฝั่ง UI:**
    -   **หน้าร้านค้า:** แสดงรายการกล่องสุ่ม
    -   **อนิเมชันเปิดกล่อง:** เอฟเฟกต์ตื่นเต้นๆ เวลาเปิดกล่องสุ่ม (ใช้ 50 tokens)
    -   **ระบบซื้อ:** หัก Token -> สุ่มตัวละคร -> บันทึกลง User

### 3.3. คลังตัวละคร (`/dashboard/blooks`)
**วัตถุประสงค์:** โชว์ของสะสมที่หามาได้

-   **งานฝั่ง UI:**
    -   ตารางแสดงตัวละครทั้งหมดที่ปลดล็อกแล้ว
    -   ปุ่ม "เลือกใช้ (Equip)" เพื่อเปลี่ยนรูปโปรไฟล์ (`user.image`) หรือตั้งค่าตัวละครหลัก
    -   แสดงเงาตัวละครที่ยังไม่ปลดล็อก เพื่อกระตุ้นให้อยากสะสม

---

## 4. ระยะที่ 3: การปรับปรุงระบบ (Phase 3: System Refinement) - **ความสำคัญต่ำ**

### 4.1. การตั้งค่าและโปรไฟล์
-   **แก้ไขโปรไฟล์:** เปลี่ยนชื่อ, เปลี่ยนรหัสผ่าน
-   **การตั้งค่า:** ปิด/เปิดเสียงประกอบ, เปลี่ยนภาษา

### 4.2. หน้าดูแลระบบ (`/admin`)
-   เข้าได้เฉพาะ `role === 'ADMIN'`
-   ดูรายชื่อผู้ใช้ทั้งหมด, ลบชุดคำถามที่ไม่เหมาะสม, ดูสถิติรวมของระบบ

---

## 5. ขั้นตอนต่อไปที่แนะนำ (Next Steps)
1.  **เริ่มทำระบบรายงานผล (Reports System):** แนะนำให้เริ่มที่ส่วนนี้ เพราะใช้ข้อมูลที่มีอยู่แล้ว และสำคัญต่อครูมาก
2.  **ระบบการบ้าน (Assignments):** ทำต่อจากรายงานผล เพื่อให้ครูใช้งานได้ครบลูป
3.  **ระบบค้นหา (Discovery):** เพื่อเปิดกว้างเนื้อหาในชุมชน
