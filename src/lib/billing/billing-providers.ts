/** Stripe API / webhooks — existing customerId field on User */
export const BILLING_PROVIDER_STRIPE = "stripe";

/** Placeholder adapters until Omise / 2C2P wired */
export const BILLING_PROVIDER_THAI_MOCK = "thai_mock";

/** Omise Thailand (PromptPay / charges) */
export const BILLING_PROVIDER_OMISE = "omise";
